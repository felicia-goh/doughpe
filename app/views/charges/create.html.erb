<div class="container">
    <div class="card-payment mt-3 text-align-center">
      <div style="border-radius:200px; height:200px; width:200px; background: #F8FAF5; margin:0 auto">
        <i class="checkmark">✓</i>
      </div>
      <h2>Successful, you paid <strong>$<%= number_with_precision(@basket.total, precision:2) %></strong>!</h2>      <ul class="list-group">
        <li class="list-group-item">Name: <%= current_user.name %></li>
        <% @orders.each do |order| %>
        <li class="list-group-item">Delivery address: <%= order.delivery_address %></li>
        <li class="list-group-item">Delivery method: <%= order.delivery_method %></li>
        <li class="list-group-item">Time slot: <%=order.time_period %></li>
        <% end %>
      </ul>
      <%= link_to "Back to home", root_path, class: "btn btn-primary my-3" %>
    </div>
  </div>

